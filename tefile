FROM kalilinux/kali-rolling

RUN apt-get update && \
    apt-get install -y systemd systemd-sysv && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Create a volume for the systemd units
VOLUME [ "/sys/fs/cgroup" ]

# Set the command to run systemd
CMD ["/lib/systemd/systemd"]
